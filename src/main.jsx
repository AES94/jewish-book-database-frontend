import React from "react";
import ReactDOM from "react-dom/client";
import JewishBookDatabase from "./JewishBookDatabase.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JewishBookDatabase />
  </React.StrictMode>
);
